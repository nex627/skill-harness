# Skill Harness 规范文档

**版本**：v1.0.0
**状态**：正式发布
**日期**：2026-07-16

---

## 1. 这是什么

Skill Harness 是一个**工程约束型 skill 创建器**。它不是一个 skill 模板下载站，而是一套帮你为业务场景创建高质量 skill 的流水线方法。

**核心问题**：AI Agent 跳步、简略输出、幻觉、产出不稳定、无法沉淀经验。

**解决方案**：PDCA 闭环 + 三层知识库 + 五大工程约束。

## 2. 差异化定位

| 项目 | 解决什么 | 方法 |
|------|---------|------|
| Anthropic skill-creator | 怎么写 SKILL.md | 模板 + 指导 |
| nuwa-skill（女娲） | 蒸馏人的思维 | 6路采集 + 3重验证 |
| 达尔文.skill | 让 skill 进化 | 自主实验循环 |
| **Skill Harness** | **为业务场景创建工程化 skill** | **PDCA + 三层知识库 + 五大约束** |

一句话：**女娲蒸馏人，Skill Harness 装配业务。**

## 3. 五大工程约束

所有 skill 创建过程必须遵守，不可跳过：

| # | 约束 | 要求 |
|:---:|------|------|
| 1 | **流程硬阻断** | PDCA 四阶段强依赖，上一阶段未通过不可进入下一步 |
| 2 | **三层知识库** | 自动读取公共库、行业库、私有库，不重复造轮子 |
| 3 | **禁止简略输出** | skill 定义必须分段、结构化，禁止一次性简略产出 |
| 4 | **人机双校验** | AI 自检 + 人工复核，草稿不可直接生效 |
| 5 | **版本可迭代** | 语义化版本号，可回溯、可复用 |

## 4. 三层知识库

```
L1: 全局公共库（全岗位共享）
    ├── PDCA/OODA 等通用流程框架
    ├── 通用模板、检查项
    └── 权限：所有 skill 只读

L2: 行业垂直库（按赛道共享）
    ├── 各行业最佳实践、竞品方案
    └── 权限：同行业可读写

L3: 业务私有库（单 skill 专属）
    ├── 历史执行记录、踩坑经验
    └── 权限：本 skill 读写
```

**冲突仲裁**：L3 > L2 > L1（自己踩过的坑优先级最高）。

## 5. PDCA 创建流程

Skill Harness 被触发后，引导用户走四步：

### P — 计划阶段

| 步骤 | 动作 | 产出 |
|:---:|------|------|
| 1 | 读取 L3 历史经验（≥3条） | 历史踩坑清单 |
| 2 | 读取 L2 行业对标（≥3个） | 竞品/最佳实践分析 |
| 3 | 整合需求，选择模板 | skill 需求规格 |

**阻断条件**：历史数据 < 3条 或 对标 < 3个（首次创建可标记"无历史"强制放行）。

### D — 执行阶段

三段式结构化产出：

| 段落 | 内容 |
|------|------|
| D1 执行摘要 | skill 定位、触发词设计、核心逻辑说明 |
| D2 详细定义 | 完整 SKILL.md（YAML frontmatter + 触发规则 + 执行逻辑 + 检查项） |
| D3 偏差记录 | 与计划的偏差、中途调整 |

**禁止**：一步式简略输出。

### C — 检查阶段

双层校验：

| 层级 | 内容 | 阈值 |
|------|------|------|
| AI 自检 | 格式校验 + 逻辑完整性 + 触发词覆盖度 | ≥80分 |
| 人工复核 | 质量达标 + 符合业务预期 | 通过 |

**驳回**：退回 D 阶段修改，不退回 P 阶段。

### A — 沉淀阶段

| 步骤 | 产出 |
|:---:|------|
| 1 | 效果评估（skill 是否可用、触发是否准确） |
| 2 | 踩坑记录（≥3条，含根因） |
| 3 | 写入 L3 私有库 |
| 4 | 生成版本号 |

## 6. Skill 类型

Skill Harness 支持创建三种类型的 skill：

| 类型 | 定义 | 适用场景 | 模板 |
|------|------|---------|------|
| **基础型** | 单一职责的执行 skill | 简单工具调用、格式转换 | `basic-skill.md` |
| **Workflow Harness 型** | PDCA 闭环 + 工程约束 | 内容运营、开发编码、产品需求 | `workflow-harness-skill.md` |
| **知识库驱动型** | 知识库 + 决策逻辑 | 投资、学习、关系决策 | `knowledge-driven-skill.md` |

## 7. 版本号规范

```
MAJOR.MINOR.PATCH（如 v1.2.3）

MAJOR: 流程骨架/约束规则变更（向后不兼容）
MINOR: 知识库扩充、检查项新增（向后兼容）
PATCH: 错别字修正、模板微调（无功能变更）
```

## 8. 兼容性

- 兼容 [Agent Skills 协议](https://agentskills.io/)
- 支持 `npx skills add` 安装
- 本质是 markdown + YAML frontmatter，不依赖特定平台
- 可在 Claude Code、Codex、Cursor、TRAE 等 50+ runtime 中运行

## 9. 禁忌规则

| 类型 | 禁忌 |
|------|------|
| 资产 | 禁止自动生成子 skill，1 业务 = 1 skill |
| 流程 | 禁止跳过对标和历史经验读取 |
| 流程 | 禁止 AI 一步式简略输出 |
| 流程 | 禁止跳过人工复核 |
| 迭代 | 禁止无复盘的一次性作业 |

## 10. 扩展预留

| Phase | 内容 | 状态 |
|-------|------|:---:|
| Phase 1 | 纯 skill 对话式交付 | ✅ 当前版本 |
| Phase 2 | 可视化面板（只读展示层） | 预留 |
| Phase 3 | 多 Agent 互检体系 | 预留 |

---

> **Skill Harness** — 为每一个业务场景，创建不可跳步、不可简化的工程级 skill。
