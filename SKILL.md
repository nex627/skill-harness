---
name: skill-harness
version: 1.0.0
description: 工程约束型 skill 创建器。为业务场景创建有 PDCA 闭环、三层知识库支撑、五大工程约束的标准化 skill。不是写一个文件，是装配一条流水线。
author: NEX
license: MIT
tags: [skill-creation, workflow, pdca, engineering]
---

# Skill Harness

## 定位

**工程约束型 skill 创建器。** 帮你为任何业务场景创建高质量、可迭代、有工程约束的 skill。

不是给你一个模板让你填空。是带你走一遍 PDCA 流程，确保产出的 skill 有流程管控、有知识库支撑、有质量校验。

## 解决什么问题

| 问题 | Skill Harness 怎么解决 |
|------|----------------------|
| AI 跳步、凭空决策 | P 阶段强制读取历史经验 + 外部对标 |
| 产出简略、不可用 | D 阶段三段式结构化产出，禁止一步式输出 |
| 草稿直接上线出事 | C 阶段 AI 自检 + 人工复核双闸口 |
| 做完不沉淀、重复踩坑 | A 阶段强制入库，经验可复用 |
| skill 越来越多越来越乱 | 1 业务 = 1 skill，禁止生成子 skill |

## 触发规则

当用户提到以下关键词时触发：

- 创建 skill、做一个 skill、新建技能、造一个 skill
- skill harness、技能创建、技能流水线
- 为 XX 场景做一个 skill、帮 XX 岗位创建 skill
- skill 工程化、skill 质量保证

## 执行流程：PDCA 四阶段

### P 阶段 — 计划（强制双源信息采集）

**目标**：杜绝凭空决策，确保信息充分。

执行顺序（固定，缺一不可）：

| 顺序 | 动作 | 说明 | 阈值 |
|:---:|------|------|------|
| 1 | **内部经验读取** | 读取 L3 私有库（历史踩坑、迭代经验）。首次创建无历史时标记"无历史"强制放行 | ≥3条 或 标记首次 |
| 2 | **外部行业对标** | 搜索同类 skill、最佳实践、竞品方案 | ≥3个对标对象 |
| 3 | **整合方案规划** | 结合用户需求 + 内部经验 + 外部对标，确定 skill 类型（基础型/Workflow Harness 型/知识库驱动型） | 完整覆盖 |

**标准产出**：

| 编号 | 名称 | 要求 |
|------|------|------|
| P0 | 双源信息调研报告 | 内部历史摘要 + 外部对标分析，≥800字 |
| P1 | skill 需求规格 | skill 名称、定位、触发词草案、目标用户、核心能力 |
| P2 | 模板选择 | 从三种模板中选择：basic / workflow-harness / knowledge-driven |

**阻断条件**：
- 对标 < 3个 → 阻断，补充后继续
- 需求不清晰 → 阻断，明确后继续
- 首次创建无历史 → 标记后放行

---

### D 阶段 — 执行（三段式结构化产出）

**目标**：结构化交付，禁止一步式简略输出。

三段式结构：

| 段落 | 内容 | 要求 |
|------|------|------|
| **D1 执行摘要** | skill 定位、触发词设计、核心执行逻辑 | ≥200字 |
| **D2 详细定义** | 完整 SKILL.md 文件 | 按模板填充所有必需模块 |
| **D3 偏差记录** | 与 P 阶段计划的偏差、中途调整 | ≥50字 |

**D2 必需模块清单**：

所有 skill 必须包含：

```yaml
# YAML frontmatter（必需）
---
name: skill-name
version: 1.0.0
description: 一句话说清这个 skill 干什么（≤300字）
author: xxx
tags: [tag1, tag2]
---
```

```markdown
# Skill 名称

## 定位
一句话说清楚这个 skill 是什么、解决什么问题。

## 触发规则
列出触发关键词。

## 执行逻辑
核心流程，按步骤写清楚。

## 检查项
执行完毕后的自检清单。

## 禁忌
不能做什么。
```

**按 skill 类型补充**：

| 类型 | 额外必需模块 |
|------|------------|
| 基础型 | 输入输出定义 + 异常处理 |
| Workflow Harness 型 | PDCA 四阶段定义 + 三层知识库引用 + 阻断规则 |
| 知识库驱动型 | 知识库路径 + 决策表 + 关联 wiki |

**阻断条件**：
- D1 < 200字 → 阻断
- D2 缺少必需模块 → 阻断
- D3 缺失 → 阻断

---

### C 阶段 — 检查（双层校验）

**目标**：杜绝草稿直接生效。

**第一层：AI 自检**

| 维度 | 检查内容 | 评分标准 |
|------|---------|---------|
| 格式规范 | YAML frontmatter 完整、Markdown 结构正确 | 0-100 |
| 逻辑完整性 | 触发词覆盖场景、执行逻辑闭环、无死胡同 | 0-100 |
| 可用性 | 用户读完能不能直接用、是否需要额外解释 | 0-100 |
| 边界清晰 | 明确标注了不能做什么 | 0-100 |

综合得分 ≥ 80 分通过，否则阻断。

**第二层：人工复核**

向用户展示自检结果 + skill 草稿，等待用户确认：
- ✅ 通过 → 进入 A 阶段
- ❌ 驳回 → 退回 D 阶段修改（附带修改意见）

**驳回规则**：退回 D 阶段，不退回 P 阶段。

---

### A 阶段 — 沉淀（经验入库）

**目标**：确保经验可沉淀、可迭代。

| 步骤 | 动作 | 产出 |
|:---:|------|------|
| 1 | 效果评估 | skill 是否可用、触发词是否准确 |
| 2 | 踩坑记录 | ≥3条，含根因分析 |
| 3 | 写入 L3 | 沉淀到私有库 |
| 4 | 版本号 | 语义化版本（v1.0.0） |

**版本号规则**：
- MAJOR：流程骨架变更（不兼容）
- MINOR：知识库扩充（兼容）
- PATCH：措辞修正（无功能变更）

---

## 三种 Skill 类型

### 基础型（basic）

适用：简单工具调用、格式转换、单一执行任务。

特征：一个触发词 → 一个执行流程 → 一个产出。不需要 PDCA 闭环。

### Workflow Harness 型

适用：内容运营、开发编码、产品需求等需要工程约束的场景。

特征：完整 PDCA 闭环 + 三层知识库 + 五大约束 + 阻断机制。

### 知识库驱动型

适用：投资决策、学习决策、关系决策等需要知识库支撑的场景。

特征：读取知识库 → 匹配决策表 → 输出建议。skill 是调用者，知识库是大脑。

---

## 三层知识库使用规则

### 读取规则（P 阶段强制）

```
读取顺序：
1. L3 私有库 → 规避历史踩坑（≥3条）
2. L2 行业库 → 对标最佳实践（≥3个）
3. L1 公共库 → 方法论加持（可选）
```

### 沉淀规则（A 阶段强制）

```
沉淀内容：
1. 踩坑记录 → 写入 L3（≥3条，含根因）
2. 成功模式 → 写入 L3
3. 通用经验 → 审核后同步 L2
```

### 冲突仲裁

```
L3（自己踩过的坑） > L2（行业共识） > L1（通用方法论）
```

---

## 五大工程约束（不可跳过）

1. **流程硬阻断**：PDCA 阶段强依赖，未通过不可进入下一步
2. **三层知识库**：P 阶段强制读取，A 阶段强制沉淀
3. **禁止简略输出**：D 阶段三段式，禁止一步式产出
4. **人机双校验**：C 阶段 AI 自检 + 人工复核
5. **版本可迭代**：语义化版本号，可回溯

---

## 禁忌规则

| 类型 | 禁忌 | 严重度 |
|------|------|:---:|
| 资产 | 禁止自动生成子 skill | 严重 |
| 资产 | 禁止 1 业务多 skill 堆砌 | 严重 |
| 流程 | 禁止跳过外部对标 | 严重 |
| 流程 | 禁止跳过历史经验读取 | 严重 |
| 流程 | 禁止 AI 一步式简略输出 | 严重 |
| 流程 | 禁止跳过人工复核 | 严重 |
| 迭代 | 禁止无复盘的一次性作业 | 严重 |

---

## 与其他 skill creator 的区别

| 维度 | skill-creator | nuwa-skill | Skill Harness |
|------|--------------|------------|---------------|
| 目标 | 写一个 SKILL.md 文件 | 蒸馏人的思维 | 为业务场景装配工程化 skill |
| 方法 | 模板 + 指导 | 6路采集 + 3重验证 | PDCA + 三层知识库 + 五大约束 |
| 产出 | 一份 skill 文件 | 人物认知模型 | 有质量保证的 skill + 版本记录 |
| 质量保证 | 无 | 保真度评分卡 | AI 自检 + 人工复核双闸口 |
| 经验沉淀 | 无 | 无 | L3 私有库自动入库 |

---

## 使用示例

```
用户 ❯ 帮我为抖音内容运营创建一个 skill

Skill Harness ❯ 好的，启动 Skill Harness 流水线。

【P 阶段 - 计划】
1. 读取历史经验...（检查 L3 私有库）
2. 搜索行业对标...（抖音内容运营最佳实践）
3. 整合需求...（确定 skill 类型：Workflow Harness 型）

需求规格：
- skill 名称：抖音内容运营
- 定位：从选题到发布的全流程内容运营 skill
- 触发词：发抖音、做视频内容、抖音运营
- 核心能力：选题 → 脚本 → 分镜 → 发布策略

请确认需求规格，确认后进入 D 阶段生成 skill 草稿。
```

---

## 版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0.0 | 2026-07-16 | 初始版本，PDCA + 三层知识库 + 五大约束 |

---

> **Skill Harness** — 不是写一个文件，是装配一条流水线。
